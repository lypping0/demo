<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.hnist.ssm.qqzone.dao.UserDAO">
     <select id="getUser" resultType="User">
      select * from t_user where loginId=#{loginId} and password=#{password}
     </select>
	  
	  <select id="getFriends" parameterType="int" resultType="User">
	    select t1.* from t_user t1 inner join t_user_friend t2 on t1.id=t2.fid where t2.uid=#{value}
	  </select>
</mapper>
